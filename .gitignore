# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Never track real backend secrets (.env.example is the tracked template)
Backend/.env
Backend/.env.*
!Backend/.env.example

# Full-resolution source photography / footage — kept locally, never bundled or tracked
design-assets/

# Credential exports and audit deliverables — never commit (public repo)
api-keys-*.csv
*.docx

# Razorpay / gateway key exports handed over for deployment — never commit
*-key.csv
*-keys.csv
